<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.dollarhide.androidmovieviewer.movieviewer" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Home"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name=".SearchActivity"
                android:label="@string/app_name"
                android:parentActivityName="org.dollarhide.androidmovieviewer.movieviewer.Home">
        </activity>
        <activity
                android:name=".InformationActivity"
                android:label="@string/app_name"
                android:parentActivityName="org.dollarhide.androidmovieviewer.movieviewer.SearchActivity">
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>
